name: BB MQ legitimate CI V8R4
on:
  pull_request:
jobs:
  gate:
    name: BB_RETRY_GATE_20260810_C741
    if: github.event.pull_request.user.login == 'mergify[bot]'
    runs-on: ubuntu-latest
    steps:
      - run: echo "legitimate merge-queue CI gate"
